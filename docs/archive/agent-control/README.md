# Agent Control Notes

`AGENTS.md` 与 `CLAUDE.md` 仍保留在仓库根目录，不迁移到 `docs/`，原因如下：

- 这两个文件属于 AI agent 行为控制入口，放置在根目录可被工具链稳定发现。
- 迁移后可能导致指令加载失效或行为偏差。
- 为保持可维护性，业务文档迁移到 `docs/`，控制文件留在根目录。

如需调整 agent 规则，请直接修改根目录对应文件并同步说明到本目录。
