# 安全策略 / Security Policy

ChronoDesk 重视负责任的漏洞披露。请勿通过公开 Issue、Discussion、Pull Request、聊天记录或社交媒体披露未修复漏洞。

## 私密报告漏洞

请使用 GitHub 的私密漏洞报告入口：

<https://github.com/seaworld008/chronodesk/security/advisories/new>

如果该入口暂时不可用，请通过[仓库所有者 GitHub 公开资料](https://github.com/seaworld008)中公布的私下联系方式联系维护者。不要为了联系维护者而新建公开 Issue，也不要将漏洞详情发送给无关第三方。

报告中请尽量包含：

- 受影响的版本、commit SHA、部署方式和配置前提。
- 漏洞类型、攻击者所需权限、影响范围和最坏后果。
- 最小可复现步骤或概念验证；请勿包含真实凭据或生产数据。
- REST、MCP `2026-07-28`、A2A `1.0` 或 Web 等受影响入口。
- Agent 身份、scope、策略、幂等键、租约、回调或不可信内容是否参与攻击链。
- 已知缓解措施，以及你希望协调披露的方式。

请只测试你拥有或已获明确授权的环境。不要进行数据破坏、持久化访问、服务拒绝、社会工程或访问其他用户数据。

## 支持范围

仓库已有正式 GitHub Release 标签，但 `1.0.0` 之前不承诺稳定兼容，支持状态也不构成向后兼容保证。维护者支持默认分支 `main` 的最新版本和最新正式 GitHub Release；更早标签、旧 commit、未合并分支、私有 fork 和未经支持的协议版本仅尽力协助。

| 版本 | 支持状态 |
| --- | --- |
| `main` 最新版本 | 支持 |
| 最新正式 GitHub Release | 支持 |
| 更早标签或 commit | 尽力而为 |
| MCP 非 `2026-07-28` / A2A 非 `1.0` | 不支持 |

第三方依赖漏洞只有在能够实际影响 ChronoDesk 时才属于本项目范围。单纯扫描器输出仍然有帮助，但请附上可达路径、运行时条件和影响分析。

## 处理与披露

维护者会在项目容量允许的情况下确认报告、复现问题、评估严重性并协调修复。响应时间不是服务等级承诺。请在双方商定公开时间前保持细节私密；修复完成后，维护者可能通过 GitHub Security Advisory、发行说明或 changelog 致谢报告者（可选择匿名）。

维护者可能在以下情况下关闭报告：无法复现、没有安全影响、仅影响不受支持版本、需要不现实的攻击前提，或属于功能/支持请求。即使如此，也会尽量说明判断依据。

## Agent 安全边界

所有 Agent、模型、工具、任务、资源、附件、回调和协议载荷均是不可信输入。提示词、客户端声明、Agent Card 或 Adapter 侧检查不能替代服务端认证授权。安全修复不得在 REST、MCP、A2A Adapter 中分别复制业务规则；应在共享领域服务中统一落实，并覆盖跨协议回归测试。
