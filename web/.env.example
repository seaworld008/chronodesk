# API Base URL. Keep `/api` when the Vite proxy is enabled.
VITE_API_URL=/api
VITE_PROXY_TARGET=http://localhost:8081

# App Configuration
VITE_APP_NAME=ChronoDesk
VITE_APP_VERSION=0.3.0

# Development Configuration
VITE_DEV_MODE=true
